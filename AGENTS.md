# Agent Instructions

## Dev instructions

- Never the run the application, focus only on editing
- Don't write too much of defensive code
- Avoid touching files the user did not ask about; if you encounter unexpected changes, pause and ask how to proceed.
- Don’t revert unrelated git changes or use destructive git commands unless explicitly instructed.

## Test instructions

- Run `ty check` to check for LSP diagnostics. Only fix those urgent.
